import { environment } from "../environments/environment.development";
export const POST_AUTH = environment.Url + "/api/Auth/Authentication";
export const PRODUCTS = environment.Url + "/api/products";
export const CATEGORY = environment.Url + "/api/categories";
